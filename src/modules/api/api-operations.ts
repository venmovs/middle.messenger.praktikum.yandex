const auth = '/auth/';
const signUp = auth + 'signup';
const signIn = auth + 'signin';
const authUser = auth + 'user';
const logout = auth + 'logout';

const authObj = {
    signUp,
    signIn,
    authUser,
    logout,
};

export { authObj };
